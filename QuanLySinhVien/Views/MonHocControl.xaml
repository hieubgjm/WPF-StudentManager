<UserControl x:Class="QuanLySinhVien.Views.MonHocControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" x:Name="txtTuKhoa"
                     ToolTip="Tìm theo mã môn hoặc tên môn"
                     TextChanged="txtTuKhoa_TextChanged" />

            <Button Grid.Column="2" Content="Làm mới" Style="{StaticResource NutPhu}"
                    Margin="0,0,8,0" Click="btnLamMoi_Click" />
            <Button Grid.Column="3" Content="Thêm" Style="{StaticResource NutChinh}"
                    Margin="0,0,8,0" Click="btnThem_Click" />
            <Button Grid.Column="4" Content="Sửa" Style="{StaticResource NutChinh}"
                    Margin="0,0,8,0" Click="btnSua_Click" />
            <Button Grid.Column="5" Content="Xóa" Style="{StaticResource NutXoa}"
                    Click="btnXoa_Click" />
        </Grid>

        <DataGrid Grid.Row="1" x:Name="dgMonHoc"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  CanUserAddRows="False"
                  AlternatingRowBackground="#F7F9FC"
                  MouseDoubleClick="dgMonHoc_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã môn" Binding="{Binding MaMonHoc}" Width="120" />
                <DataGridTextColumn Header="Tên môn học" Binding="{Binding TenMonHoc}" Width="240" />
                <DataGridTextColumn Header="Số tín chỉ" Binding="{Binding SoTinChi}" Width="100" />
                <DataGridTextColumn Header="Ghi chú" Binding="{Binding GhiChu}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
