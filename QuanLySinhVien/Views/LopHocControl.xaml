<UserControl x:Class="QuanLySinhVien.Views.LopHocControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" x:Name="txtTuKhoa"
                     ToolTip="Tìm theo mã lớp hoặc tên lớp"
                     TextChanged="txtTuKhoa_TextChanged" />

            <Button Grid.Column="2" Content="Làm mới" Style="{StaticResource NutPhu}"
                    Margin="0,0,8,0" Click="btnLamMoi_Click" />
            <Button Grid.Column="3" Content="Thêm" Style="{StaticResource NutChinh}"
                    Margin="0,0,8,0" Click="btnThem_Click" />
            <Button Grid.Column="4" Content="Sửa" Style="{StaticResource NutChinh}"
                    Margin="0,0,8,0" Click="btnSua_Click" />
            <Button Grid.Column="5" Content="Xóa" Style="{StaticResource NutXoa}"
                    Click="btnXoa_Click" />
        </Grid>

        <DataGrid Grid.Row="1" x:Name="dgLop"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  CanUserAddRows="False"
                  AlternatingRowBackground="#F7F9FC"
                  MouseDoubleClick="dgLop_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã lớp" Binding="{Binding MaLop}" Width="120" />
                <DataGridTextColumn Header="Tên lớp" Binding="{Binding TenLop}" Width="180" />
                <DataGridTextColumn Header="Khoa" Binding="{Binding Khoa}" Width="180" />
                <DataGridTextColumn Header="Sĩ số tối đa" Binding="{Binding SiSoToiDa}" Width="100" />
                <DataGridTextColumn Header="Ghi chú" Binding="{Binding GhiChu}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
