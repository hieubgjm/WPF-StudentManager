<Window x:Class="QuanLySinhVien.Views.ThemSuaLopWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm lớp học"
        Height="420" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Mã lớp" Margin="0,0,0,4" />
            <TextBox x:Name="txtMaLop" Margin="0,0,0,12" />

            <TextBlock Text="Tên lớp" Margin="0,0,0,4" />
            <TextBox x:Name="txtTenLop" Margin="0,0,0,12" />

            <TextBlock Text="Khoa" Margin="0,0,0,4" />
            <TextBox x:Name="txtKhoa" Margin="0,0,0,12" />

            <TextBlock Text="Sĩ số tối đa" Margin="0,0,0,4" />
            <TextBox x:Name="txtSiSoToiDa" Margin="0,0,0,12" />

            <TextBlock Text="Ghi chú" Margin="0,0,0,4" />
            <TextBox x:Name="txtGhiChu" Height="60" TextWrapping="Wrap" AcceptsReturn="True" />

            <TextBlock x:Name="lblThongBao"
                       Foreground="{StaticResource MauNguyHiem}"
                       TextWrapping="Wrap"
                       Margin="0,10,0,0"
                       Visibility="Collapsed" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Hủy" Style="{StaticResource NutPhu}" Width="100" Margin="0,0,8,0"
                    Click="btnHuy_Click" />
            <Button Content="Lưu" Style="{StaticResource NutChinh}" Width="100"
                    Click="btnLuu_Click" />
        </StackPanel>
    </Grid>
</Window>
